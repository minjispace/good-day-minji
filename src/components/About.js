import React from 'react';
import {AboutWrapper} from '../styles/about';

function About({id}) {
  return (
    <AboutWrapper id={id}>
      <h1>about</h1>
    </AboutWrapper>
  );
}

export default About;
